// Package automation authorizes metadata-only image assessments and refreshes
// existing deletion checks. It consumes GitHub metadata through the GitHub CLI;
// candidate code, cloud credentials, and deletion verdicts remain outside it.
package automation
